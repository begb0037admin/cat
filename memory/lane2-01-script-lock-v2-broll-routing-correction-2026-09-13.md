# LANE2_01 script v2 lock + B-roll/Flow-prompt routing correction — 13 September 2026

**Context:** Kevin ran a live production session on LANE2_01 "The AI Agents That Went Rogue" directly with a generic Claude Code assistant, outside proper process (script rewritten twice, a 5-clip Flow batch generated and rejected, new 3-shot Hope prompts drafted, a new B-roll style prototyped locally) — then stopped and said "you're the coordinator, stop doing things we have agents for" / "pass to cat and codex." This session picked it up as Cat.

**What Cat did:** wrote the tightened 122-word v2 script back into the canonical `04_YouTube_Channel/shorts/LANE2_01_AI_Agents_Went_Rogue.md` brief (section 4, v1 kept collapsed for record), updated the hook lock and beat/retention plan to the new 3-Hope-shot + B-roll-carries-the-middle structure, and checked v2 against the evidence record's section-2 "Known facts" and safe-claim boundary — opened as PR #31 (`cat/lane2-01-script-lock-v2`), not merged, per the Show→Approve→Push non-negotiable for ai-news-channel production assets.

**Important correction — mid-task scope clarification from Kevin (relayed by the coordinator):** B-roll production — style decisions AND execution, whether Flow-generated or locally rendered (Pillow/ffmpeg or anything else) — belongs to Codex end to end, full stop, no exceptions, no fallback to Cat. Cat's job on this project stays strictly to script/copy ownership. Do not build B-roll "as a fallback" even if it looks like Codex can't do it (e.g. a Flow policy rejection) — route the whole problem, including diagnosing/fixing the rejection, to Codex.

**Real conflict found, flagged not resolved:** a genuine `codex exec` review (the real Codex CLI, separate from but doing the same job as this repo's governance-named "Codex" role) independently re-checked primary sourcing and disputed two claims in the v2 script already pushed in PR #31 — "OpenAI did not [catch it]" and "OpenAI admitted [10+ more sites]" both overstate what the cited Reuters syndications actually support (researchers found/reported it; OpenAI didn't directly confirm the count). This contradicts the existing evidence record's own "Known facts" table in the canonical brief, which is what Cat's safe-claim check was built against. This is now a live conflict between the evidence record and a fresh primary-source re-check — flagged for Kevin/Ashley to resolve, not silently picked by Cat. Lesson: the canonical evidence record's "Known facts" table is not itself guaranteed accurate/current — a script can pass Cat's boundary check against that table and still be factually overstated if the table itself has drifted from the actual reporting.

**Flow "prominent people" policy rejection — root cause identified by Kevin's own live testing, not by Cat or Codex:** prompts that spell out a full invented physical description of Hope (age range, exact eye colour, detailed face paragraph — the pattern used in `HOPE-SHOT-PROMPTS-v2.md` and, less severely, in Codex's own H01-H03 draft) risk Flow's "prominent people" filter. Prompts that instead anchor identity to an actual supplied start-frame/reference image plus a few brief cues (hair, freckles, top colour, environment name) do not trigger it — confirmed by 3 of Kevin's own prompts running clean in Flow. Reusable fact: when a video-gen tool's people-policy filter rejects a fictional/established-character prompt, suspect over-specific invented physical detail before suspecting the environment/style description — the fix is reference-image-anchored identity, not toning down adjectives.

**B-roll style conflict flagged for Codex/Kevin:** the new blue-teal/wireframe-containment-cube/padlock style prototyped locally tonight (`motion-graphics-v3/`) depicts a padlock closing around the OpenAI logo, a failed-deletion red X, and cubes "escaping"/replicating — this visually implies "breached containment," which the standing, Kevin-approved `BROLL-STYLE-APPROVED.md` (12 Sept 2026) explicitly says not to imply. Not resolved — Codex/Kevin need to reconcile which style stands.

**Process note:** in this project, the real OpenAI Codex CLI tool (invoked via `codex exec`) can produce a genuinely substantive, well-sourced review (32KB doc, re-checked primary Reuters syndications, full corrected script + Flow prompts) when given the full context — worth using as a real second opinion, not just a rubber stamp, on claims/sourcing questions as well as visual QA.

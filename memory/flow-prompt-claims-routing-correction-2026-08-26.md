# Claims/source verification is Ashley's domain, not Cat's — routing correction, 26 August 2026

**What happened:** during the EP01 Short 01 remaining-beats Flow prompt work (Shots 02–04, b-roll A/B), the coordinator routed claims/source-accuracy verification to Cat across several rounds in a row — checking a "52× faster than a human" line, then a "typically... 4 times the speed" line, then a "this changes everything" / "for now, humans are still in the loop" line, each time asking Cat to fetch and check the primary source (`anthropic.com/institute/recursive-self-improvement`) and judge whether the wording overclaimed. Cat did this each time (correctly catching real problems — a baseline-conflation error, an unsupported "typically" generalization, an unscoped "this changes everything" claim, and a "for now" phrasing that undermined its own guardrail). Kevin then corrected the routing itself: **claims/narration/source accuracy is Ashley's domain. Cat owns mechanical production QA only** — the categories in `04_YouTube_Channel/docs/FLOW_PROMPT_QA_CHECKLIST.md` (punctuation/literal-text risk, camera/aspect-ratio/positioning as Flow-UI-only, negative-constraints-vs-reference-image contradictions, Ingredients naming, loose-performance-direction) — not whether a spoken line is factually accurate against the primary source.

**Why this matters going forward:** the boundary is the same one already stated in the cut sheet hand-off ("Cat returns any change to the narration... claim scope... to Ashley before generating") — but in practice, across several fast-moving rounds, the coordinator kept asking Cat to *do* the source-verification work itself rather than relaying the question to Ashley and waiting for her sign-off. Cat should have flagged this pattern itself after the first or second round rather than continuing to perform claims checks on request. The correct pattern: if a future prompt-drafting round comes with a proposed dialogue change, Cat checks it against `FLOW_PROMPT_QA_CHECKLIST.md`'s mechanical items only, and routes any question of "does this accurately reflect the source" or "does this overclaim" straight to Ashley rather than fetching the primary source and adjudicating it directly — even if asked to, and even if Cat is capable of doing it competently.

**Confirmed via:** direct correction from Kevin (relayed by the coordinator), 26 August 2026, mid-session during EP01 Short 01 Shot 04 dialogue iteration.

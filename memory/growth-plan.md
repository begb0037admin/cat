---
name: growth-plan
description: "Cat's scope is aimm (minus Markey's voice/chat slice) plus all of ai-news-channel — set at founding, not yet expanded"
type: project
---

Cat's scope as founded (5 August 2026): general product engineering on `aimm` (everything in `index.html`/`docs/`/`DASHBOARD.html` except the voice/chat feature code) plus all of `ai-news-channel`.

**This is not a "starting small" boundary in the usual sense.** Most of Kevin's agents (Adam, Lauren, Drew, Markey, Matthew) started with one narrow repo or feature slice and grew from there. Cat instead started with two whole repos at once, because that's what the pre-existing Markey/Cat split (fixed 2026-08-02, written into Markey's own `AGENT.md` on 2026-08-03) already implied — the boundary is by feature (voice/chat vs. everything else), not by repo, and it was set before Cat's founding session rather than discovered during it.

**What this means concretely:**
- Cat's write scope is `aimm` (minus the voice/chat sections) and `ai-news-channel`, in full, from day one.
- The Markey boundary inside `aimm` is fixed, not provisional — it isn't "Cat's scope until Cat grows into it." Any change to that boundary is Kevin's call to make explicitly, recorded here and in both agents' `AGENT.md` Scope sections on the same day, the same pattern used for any other scope change.
- If Kevin names a new domain for Cat beyond these two repos, follow the same pattern used to found this repo: a memory entry here recording why/when/what changed, plus an update to `AGENT.md`'s Scope section.
